import React from 'react';

function Fragments(props) {
    return (
        <React.Fragment>
            <h1>React Fragments</h1>
            <p>React Fragments let you group a list of children without adding extra nodes to the DOM.</p>
        </React.Fragment>
    );
}

export default Fragments;